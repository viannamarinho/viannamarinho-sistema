'use client'

import { useState } from 'react'
import { InputCheckBox } from '@/styles/inputs'

export function CheckBox() {
  const [inputValue, setInputValue] = useState('')

  return <InputCheckBox>InputText</InputCheckBox>
}
