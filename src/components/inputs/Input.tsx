'use client'

import { useState } from 'react'
import { InputCustom } from '@/styles/inputs'

export function Input() {
  const [inputValue, setInputValue] = useState('')

  return <InputCustom>InputText</InputCustom>
}
